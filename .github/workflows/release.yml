# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: RELEASE

on:  
  release:
    types: [created]
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version: [ 11  ]

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK ${{ matrix.version }}
        uses: actions/setup-java@v2
        with:
          java-version: '${{ matrix.version }}'
          distribution: 'adopt'
          cache: maven
      - name: Change Version of Release version
        run: mvn -B versions:set -DnewVersion=1.0.${{github.run_number}} --file pom.xml -P java${{ matrix.version }}

      - name: Build with Maven
        run: mvn -B package --file pom.xml -P java${{ matrix.version }}

      - name: Publish to GitHub Packages Apache Maven
        run: mvn deploy -P java${{ matrix.version }} -s $GITHUB_WORKSPACE/settings.xml -DskipTests=true
        env:
          GITHUB_TOKEN: ${{ secrets.DEPLOY }}

